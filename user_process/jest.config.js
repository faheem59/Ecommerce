module.exports = {
    testMatch: ['<rootDir>/__tests__/**/*.test.js'],  // Adjust path as needed
    // Optionally, if using TypeScript:
    // testMatch: ['<rootDir>/__tests__/**/*.test.ts'],
    testEnvironment: 'node',  // Or 'jsdom' for browser-like environment
};
